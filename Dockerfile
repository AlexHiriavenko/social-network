# Используем официальный образ Nginx как базовый образ
FROM nginx:latest

# Копируем файлы вашего React-приложения в каталог Nginx для статических файлов
COPY ./dist /usr/share/nginx/html

# Указываем порт, который Nginx должен слушать (по умолчанию 80)
EXPOSE 80

# Команда, которая будет выполнена при запуске контейнера
CMD ["nginx", "-g", "daemon off;"]